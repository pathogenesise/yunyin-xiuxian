# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-lock.yaml
pnpm-workspace.yaml
lerna-debug.log*
CLAUDE.md
AGENTS.md
chars.txt
extract_chars.py

node_modules
dist
dist-ssr
dist-electron
pkg
# docs/ 默认不入库(本机审计文档,原规则);pplmx 的设计稿目录单独放行。
# 合并 pplmx 时这里曾被整体放开,本机的 曲线审计.md / 审计规范.md 会被 git add -A 卷进去
docs/*
!docs/superpowers/
docs/generated/
*.local

# Editor directories and files
.claude/*
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Python runtime cache
__pycache__/
*.pyc

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
# 本仓库构建不需要任何环境变量;.env.* 一律不上库(若将来需要模板,再补 !.env.example)
